% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_hex_files.R
\name{ingest_hex_file}
\alias{ingest_hex_file}
\alias{load_hex_files}
\title{Load Sea-Bird HEX headers}
\usage{
ingest_hex_file(file_path)

load_hex_files(directory = ".", pattern = "\\\\.hex$", progress = TRUE)
}
\arguments{
\item{file_path}{Character. Path to a single \code{.hex} file to ingest.}

\item{directory}{Character. Directory containing \code{.hex} files (non-recursive).}

\item{pattern}{Character. Regex used to identify hex files (default \code{"\\\\.hex$"}).}

\item{progress}{Logical. Emit progress messages when \code{TRUE} (default \code{TRUE}).}
}
\value{
\itemize{
\item \code{ingest_hex_file()} returns a one-row tibble with columns: \code{file_path},
\code{file_name}, \code{header_file_name} (as written in the file header), \code{ship},
\code{cruise}, \code{tow}, and \code{net_size}.
\item \code{load_hex_files()} returns a tibble with rows bound across all successfully
ingested files. Missing fields are filled with \code{NA} and trigger warnings.
}
}
\description{
Read Sea-Bird \code{.hex} files (MOCNESS configuration captures) and extract
header metadata only. No scan payload is parsed. The function pair includes
a single-file ingester and a batch loader.
}
\details{
\itemize{
\item Header lines may begin with any number of leading asterisks; matching is
case-insensitive and tolerant to extra whitespace.
\item If required fields are absent, the ingester issues a warning and returns
\code{NA} for those columns instead of failing.
\item The batch loader scans the target directory only (no recursion), warns on
per-file failures, and is silent unless \code{progress = TRUE}.
}
}
\examples{
\dontrun{
# Single file
ingest_hex_file("path/to/M28_01.hex")

# Batch
load_hex_files(directory = "path/to/hex/")
}

}
