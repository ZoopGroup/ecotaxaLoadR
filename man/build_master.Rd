% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_master.R
\name{build_master}
\alias{build_master}
\title{Build master tow summary}
\usage{
build_master(pro_data_file, hex_data_file)
}
\arguments{
\item{pro_data_file}{Data frame produced by \code{\link[=load_pro_files]{load_pro_files()}}. Should
include \code{vessel}, \code{cruise_id}, \code{tow_number}, \code{net}, \code{lat}, \code{lon} (or
\code{long}), \code{CTDDEPTH(M)}, \code{vol}, \code{datetime_gmt}, \code{datetime_local}, and
optionally \code{is_day}.}

\item{hex_data_file}{Data frame produced by \code{\link[=load_hex_files]{load_hex_files()}} containing at
minimum \code{tow} and \code{net_size} columns.}
}
\value{
A tibble with one row per tow-number and net combination containing:
\code{vessel}, \code{cruise}, \code{tow}, \code{net}, \code{lat_decimal}, \code{long_decimal},
\code{min_depth}, \code{max_depth}, \code{volume_filtered}, \code{time_start_gmt},
\code{time_start_local}, \code{is_day}, and \code{net_size}.
}
\description{
Summarize PRO-derived cast data by tow and net, then append HEX header
metadata to produce a master table for downstream analyses.
}
\details{
Warnings are emitted once per call when:
\itemize{
\item \code{is_day} is absent (the column is added and filled with \code{NA}).
\item Multiple distinct \code{is_day} values exist within a tow/net group (the first
value is kept).
}
}
\examples{
\dontrun{
pro_data <- ecotaxaLoadR::load_pro_files(
  directory = "path/to/pro/",
  daynight  = TRUE
)

hex_data <- ecotaxaLoadR::load_hex_files(
  directory = "path/to/hex/"
)

master <- ecotaxaLoadR::build_master(
  pro_data_file = pro_data,
  hex_data_file = hex_data
)
}

}
